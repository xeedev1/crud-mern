// install mysql
//create a new schema
    // CREATE SCHEMA `test` ;
// create a table in mySQL workbench
//     CREATE TABLE `test`.`new_table` (
//     `id` INT NOT NULL,
//     `title` VARCHAR(45) NOT NULL,
//     `description` VARCHAR(255) NOT NULL,
//     `cover` VARCHAR(45) NULL,
//     PRIMARY KEY (`id`));
// add a new product in the table
    // INSERT INTO `test`.`books` (`id`, `desc`, `title`, `cover`) VALUES ('2', 'new desc', 'test title 2', 'cover.jpg');

backend
// initialize node js application 
        // npm init -y  
